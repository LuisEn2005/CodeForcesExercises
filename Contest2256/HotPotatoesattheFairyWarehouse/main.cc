#include <bits/stdc++.h>
using namespace std;

int t, n, k, pot;

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  
  cin >> t;

  while(t--){
    cin >> n;
    cin >> k;
    
    vector<int> a(2*n);

    for(int i = 0; i < 2*n; ++i){
      cin >> pot;
      a[i] = pot;
    }

    
  }

  return 0;
}
